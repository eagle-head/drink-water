import React from "react";

import { Text, View } from "react-native";

export const SettingsScreen: RNElement = (): JSX.Element => {
  return (
    <View>
      <Text>Settings</Text>
    </View>
  );
};
